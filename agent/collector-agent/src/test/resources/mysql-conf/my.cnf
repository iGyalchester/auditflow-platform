# Replaces Testcontainers' built-in MySQL config override, which still
# sets innodb_log_file_size - a variable MySQL 8.4+ removed, so mysql:9
# refuses to start with it. Supplying our own file also lets the test
# enable the general log at startup instead of via SET GLOBAL.
[mysqld]
general_log=ON
log_output=TABLE
